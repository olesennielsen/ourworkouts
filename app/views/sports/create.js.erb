/* Add the new comment to the top of the messages list */
$("<%= escape_javascript(render @sport) %>").hide().prependTo("#sports").fadeIn(1000);

/* Reset the message form */
$("#new_sport")[0].reset();