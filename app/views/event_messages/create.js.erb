/* Add the new comment to the top of the messages list */
$("<%= escape_javascript(render @event_message) %>").hide().prependTo("#event-messages").fadeIn(1000);

/* Reset the message form */
$("#new_event_message")[0].reset();