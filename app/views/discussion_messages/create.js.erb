/* Add the new comment to the bottom of the messages list */
$("<%= escape_javascript(render @discussion_message) %>").hide().prependTo("#discussion_messages").fadeIn(1000);

/* Reset the message form */
$("#new_discussion_message")[0].reset();