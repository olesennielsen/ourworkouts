$("<%= escape_javascript(render :partial => 'entry', :locals => { :user => @entry.user.name, :joined => @entry.created_at }) %>").hide().prependTo("#entries").fadeIn(100);

$("#join-button").children().remove();
$("#join-button").html("<p>You've already joined</p>");