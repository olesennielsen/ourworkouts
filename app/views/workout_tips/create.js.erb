/* Add the new comment to the top of the messages list */
$("<%= escape_javascript(render @workout_tip) %>").hide().prependTo("#workout-tips").fadeIn(1000);

/* Reset the message form */
$("#new_workout_tip")[0].reset();